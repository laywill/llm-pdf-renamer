[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "--cov=file_rename --cov-report=term-missing"

[tool.coverage.report]
exclude_lines = [
    "if __name__ == .__main__.:",
]
fail_under = 90
